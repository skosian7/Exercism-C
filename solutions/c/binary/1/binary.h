#ifndef BINARY_H
#define BINARY_H

#define INVALID -1

int convert(const char *input);
int length(const char *input);
int power_of_2(int power);
int valid(char c);
int find_head(const char *input, int length);

#endif
